import { TonerPipePipe } from './toner-pipe.pipe';

describe('TonerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TonerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
