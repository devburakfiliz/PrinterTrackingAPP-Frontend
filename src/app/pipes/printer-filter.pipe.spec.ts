import { PrinterFilterPipe } from './printer-filter.pipe';

describe('PrinterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PrinterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
