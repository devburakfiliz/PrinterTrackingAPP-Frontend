import { TonerTrackingFilterPipe } from './toner-tracking-filter.pipe';

describe('TonerTrackingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TonerTrackingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
